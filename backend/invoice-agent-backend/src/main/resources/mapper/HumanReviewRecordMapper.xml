<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="invoice_agent_backend.mapper.HumanReviewRecordMapper">

    <resultMap id="HumanReviewRecordResultMap"
               type="invoice_agent_backend.entity.HumanReviewRecord">

        <id property="id" column="id"/>

        <result property="taskId" column="task_id"/>
        <result property="decision" column="decision"/>
        <result property="reviewer" column="reviewer"/>
        <result property="reviewComment" column="review_comment"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insertHumanReviewRecord"
            parameterType="invoice_agent_backend.entity.HumanReviewRecord"
            useGeneratedKeys="true"
            keyProperty="id">

        INSERT INTO human_review_record (
        task_id,
        decision,
        reviewer,
        review_comment
        )
        VALUES (
        #{taskId},
        #{decision},
        #{reviewer},
        #{reviewComment}
        )

    </insert>

    <select id="selectByTaskId"
            parameterType="long"
            resultMap="HumanReviewRecordResultMap">

        SELECT
        id,
        task_id,
        decision,
        reviewer,
        review_comment,
        created_at
        FROM human_review_record
        WHERE task_id = #{taskId}
        ORDER BY id ASC

    </select>

</mapper>